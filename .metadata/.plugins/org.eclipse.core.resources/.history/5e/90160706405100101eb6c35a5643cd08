<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <title>Registrar Categoría</title>
	<link rel="stylesheet" th:href="@{/css/bootstrap.min.css}">
	<link rel="stylesheet" th:href="@{/css/estilos.css}">
</head>
<body>
<div class="container mt-5 col-md-6">

    <h3 class="text-center mb-4">Registrar Nueva Categoría</h3>

    <!-- ALERTA -->
    <div th:if="${mensaje}" class="alert alert-success alert-dismissible fade show" role="alert">
        <span th:text="${mensaje}"></span>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>

    <form th:action="@{/admin/registrarCategoria}" th:object="${categorias}" method="post" class="border p-4 rounded shadow-sm bg-light">
        <div class="mb-3">
            <label for="nomCate" class="form-label">Nombre de Categoría</label>
            <input type="text" id="nomCate" class="form-control" th:field="*{nomCate}" placeholder="Ej. Lácteos" required>
        </div>

        <div class="text-end">
            <button type="submit" class="btn btn-success">Guardar</button>
            <a th:href="@{/admin/inicio?seccion=productos}" class="btn btn-secondary ms-2">Volver</a>
        </div>
    </form>

</div>

<!-- Scripts Bootstrap (opcional si no tienes ya cargado globalmente) -->
<script th:src="@{/js/bootstrap.bundle.min.js}"></script>
</body>
</html>


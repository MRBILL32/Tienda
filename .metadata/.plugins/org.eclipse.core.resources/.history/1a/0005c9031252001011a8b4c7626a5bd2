<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Detalle del Producto</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/global-style.css">
</head>
<body class="bg-light">

<div class="container mt-5">
    <div class="card shadow-sm p-4">
        <h2 class="mb-3 text-primary" th:text="${producto.nomProd}">Nombre del Producto</h2>

        <ul class="list-group list-group-flush mb-3">
            <li class="list-group-item">
                <strong>Marca:</strong> <span th:text="${producto.marcaProd}">Marca</span>
            </li>
            <li class="list-group-item">
                <strong>Precio:</strong> S/. <span th:text="${producto.precioUnit}">0.00</span>
            </li>
            <li class="list-group-item">
                <strong>Stock disponible:</strong> <span th:text="${producto.stock}">0</span>
            </li>
        </ul>

        <form method="post" th:action="@{/cliente/agregarCarrito}" class="row g-3">
            <input type="hidden" name="idProd" th:value="${producto.idProd}">

            <div class="col-auto">
                <label for="cantidad" class="form-label">Cantidad:</label>
                <input type="number" class="form-control" name="cantidad" id="cantidad"
                       min="1" th:attr="max=${producto.stock}" th:value="${cantidad}" required>
            </div>

            <div class="col-auto d-flex align-items-end">
                <button type="submit" class="btn btn-success">🛒 Agregar al carrito</button>
            </div>
        </form>

        <div class="mt-4">
            <a th:href="@{/cliente/inicio}" class="btn btn-secondary">⬅ Volver a Productos</a>
        </div>
    </div>
</div>

</body>
</html>
